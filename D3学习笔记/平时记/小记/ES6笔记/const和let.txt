let:变量声明，只在代码块内有效；

使用let会在当前块级作用域内 造成暂时性死区：不会受到外界作用域的影响。

不允许重复声明(相同作用域捏。)，按照先后的顺序声明。

使用let行成的块级作用域则可以在内层作用域定义外层作用域相同的变量名


const命令 声明一个只读的常量，然后声明的同时就得赋值，否则就会报错。
const的作用域和let命令相同，只在声明的块级作用域中有效；
声明的常量也是不被提升，同样存在暂时性死区。只能在声明后面使用。

